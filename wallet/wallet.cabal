cabal-version: >=1.2
name:          wallet
version:       0
build-type:    Simple

executable wallet
  -- global
  build-depends:
      base
    , optparse-generic

  -- project
  -- build-depends: stellar-horizon
  main-is:       Main.hs
